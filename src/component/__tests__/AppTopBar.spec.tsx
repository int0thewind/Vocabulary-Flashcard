import React from 'react';

describe('AppTopBar has correct routings.', () => {
  it('AppTopBar can route to correct endpoints when user is signed in.', () => {

  });

  it('AppTopBar can route to correct endpoints when user is signed out.', () => {

  });
});

describe('AppTopBar renders correct info on desktop.', () => {
  it('AppTopBar renderes correct buttons when user is signed in.', () => {

  });

  it('AppTopBar renderes correct buttons when user is signed out.', () => {

  });
});

describe('AppTopBar renders correct info on mobile.', () => {
  it('AppTopBar renderes correct buttons when user is signed in.', () => {

  });

  it('AppTopBar renderes correct buttons when user is signed out.', () => {

  });
});

describe('AppTopBar renders correct info when viewport has changed.', () => {
  it('AppTopBar properly closed drawers when switching from mobile to desktop.', () => {

  });
});
