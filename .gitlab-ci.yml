image: node

stages:
  - build
  - lint
  - test

cache:
  paths:
    - node_modules/
    - /root/.cache/firebase/emulators

build:
  stage: build
  script:
    - yarn install

lint:
  stage: lint
  script:
    - yarn run lint

unittest_and_e2e:
  stage: test
  before_script:
    - echo $FIREBASE_TOKEN
    - apt-get update && apt-get install -y openjdk-8-jre
    - npm i -g firebase-tools
  script:
    - firebase emulators:exec --token $FIREBASE_TOKEN --only firestore 'yarn run test:sec'
    - yarn run dev &
    - firebase emulators:exec --token $FIREBASE_TOKEN --import=firebase-emulators-data 'yarn run test:e2e'
