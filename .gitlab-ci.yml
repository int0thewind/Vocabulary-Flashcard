image: node

stages:
  - build
  - lint
  - test

cache:
  paths:
    - node_modules/

build:
  stage: build
  script:
    - yarn install
    - yarn run build

lint:
  stage: lint
  script:
    - yarn run lint

unittest_and_e2e:
  stage: test
  before_script:
    - apt-get update && apt-get install -y openjdk-8-jre
    - npm install -g firebase-tools
  script:
    - firebase emulators:exec --only firestore  'yarn run test:src'
    - yarn run dev &
    - firebase emulators:exec --import=firebase-emulators-data 'yarn run test:e2e'
